/* /politika/ — политика обработки персональных данных.

   Зачем: на сайте три формы и попап, каждая собирает имя и телефон, а текст
   согласия ссылался на «политику конфиденциальности», которой не существовало.
   Ссылка в футере вела на «#». Для посетителя, который оставляет телефон, это
   ровно тот момент, когда он проверяет, кому отдаёт данные, — и не находит
   ничего. Плюс требование 152-ФЗ: оператор обязан опубликовать документ,
   определяющий его политику обработки персональных данных.

   ВАЖНО при правке: OPERATOR ниже — единственное место с реквизитами. Когда
   владелец подтвердит юридический статус (самозанятый / ИП) и ИНН, дописать
   их в inn и status — они автоматически появятся и на странице, и в футере
   через layout.mjs. Пока полей нет, блок реквизитов их просто не выводит:
   пустая строка «ИНН: —» хуже отсутствующей.
*/
import { SITE, breadcrumbs, OPERATOR } from '../layout.mjs';

const UPDATED_ISO = '2026-08-15';
const UPDATED_HUMAN = '15 августа 2026 года';

export const meta = {
  path: '/politika/',
  title: 'Политика обработки персональных данных — Даниил Карацапов',
  description: 'Какие данные собирает сайт karatsapov.ru, зачем они нужны, кому передаются и как отозвать согласие на их обработку.',
  ogImage: '/assets/og/index.jpg',
};

/* Разделы политики. Массив, а не сплошная простыня HTML: так у каждого пункта
   есть якорь, на который можно сослаться из письма или переписки. */
const SECTIONS = [
  ['obshchie', 'Общие положения', `
    <p>Настоящая политика определяет порядок обработки персональных данных
    посетителей сайта <a href="${SITE}/">karatsapov.ru</a> и действует в
    отношении всей информации, которую оператор может получить о посетителе во
    время использования сайта.</p>
    <p>Оператор персональных данных — ${OPERATOR.name}${OPERATOR.status ? `, ${OPERATOR.status}` : ''}${OPERATOR.inn ? ` (ИНН ${OPERATOR.inn})` : ''}.
    Связаться по любым вопросам об обработке данных можно по адресу
    <a href="mailto:${OPERATOR.email}">${OPERATOR.email}</a>.</p>
    <p>Используя сайт и отправляя данные через формы, посетитель подтверждает
    согласие с условиями этой политики. Если вы не согласны с ними — не
    заполняйте формы и не отправляйте свои данные.</p>
  `],

  ['sostav', 'Какие данные обрабатываются', `
    <p>Оператор обрабатывает только те данные, которые вы сообщаете сами, и
    обезличенную статистику посещений.</p>
    <p><strong>Данные, которые вы вводите в формы:</strong></p>
    <ul>
      <li>имя или обращение;</li>
      <li>номер телефона;</li>
      <li>адрес сайта — если вы указали его сами;</li>
      <li>текст комментария или описания задачи — если вы его написали.</li>
    </ul>
    <p><strong>Данные, которые собираются автоматически</strong> счётчиком
    Яндекс.Метрики: IP-адрес, данные из cookie, сведения о браузере и
    устройстве, время посещения, просмотренные страницы, источник перехода.
    Эти данные обезличены и не позволяют идентифицировать вас лично.</p>
    <p>Оператор <strong>не собирает</strong> специальные категории персональных
    данных (о состоянии здоровья, национальности, религиозных убеждениях,
    судимости) и биометрические данные.</p>
  `],

  ['celi', 'Зачем эти данные нужны', `
    <ul>
      <li><strong>Связаться с вами</strong> по оставленной заявке: перезвонить,
      написать в мессенджер, обсудить задачу.</li>
      <li><strong>Подготовить предложение</strong> по услугам и ответить на
      вопросы.</li>
      <li><strong>Улучшать сайт</strong> — на основании обезличенной статистики
      о том, какие страницы читают и откуда приходят посетители.</li>
    </ul>
    <p>Оператор не использует ваши данные для рекламных рассылок и не продаёт
    их третьим лицам.</p>
  `],

  ['osnovaniya', 'На каком основании', `
    <p>Обработка персональных данных ведётся на основании вашего согласия,
    которое вы даёте, отмечая соответствующий пункт при отправке формы, — в
    соответствии с Федеральным законом № 152-ФЗ «О персональных данных».</p>
    <p>Правовым основанием также служат договор или переговоры о его
    заключении, если вы обратились за услугой.</p>
  `],

  ['peredacha', 'Кому передаются данные', `
    <p>Оператор обрабатывает данные лично. Данные могут попадать в сервисы,
    без которых сайт не работает:</p>
    <ul>
      <li><strong>Яндекс.Метрика</strong> (ООО «Яндекс») — обезличенная
      статистика посещений. Условия использования опубликованы на сайте
      Яндекса.</li>
      <li><strong>Telegram</strong> — служит каналом доставки заявок оператору,
      через него передаются имя, телефон и комментарий из формы.</li>
      <li><strong>Хостинг сайта</strong> — в объёме технических логов
      обращений.</li>
    </ul>
    <p>Государственным органам данные передаются только по основаниям и в
    порядке, установленным законом.</p>
  `],

  ['sroki', 'Сколько данные хранятся', `
    <p>Данные из заявок хранятся до достижения цели обработки — то есть пока
    идёт общение по задаче и в течение срока действия договора, если он
    заключён, — либо до отзыва согласия.</p>
    <p>Обезличенная статистика Яндекс.Метрики хранится в соответствии со
    сроками, установленными самим сервисом.</p>
    <p>После отзыва согласия или достижения цели данные удаляются, за
    исключением сведений, которые оператор обязан хранить по закону.</p>
  `],

  ['prava', 'Ваши права и как отозвать согласие', `
    <p>Вы в любой момент вправе:</p>
    <ul>
      <li>получить сведения о том, какие ваши данные обрабатываются;</li>
      <li>потребовать уточнить, заблокировать или удалить их, если они неполны,
      устарели, неточны или получены незаконно;</li>
      <li>отозвать согласие на обработку.</li>
    </ul>
    <p>Для этого напишите на <a href="mailto:${OPERATOR.email}">${OPERATOR.email}</a>
    с темой «Персональные данные». Оператор рассмотрит обращение и ответит в
    срок, установленный законом — не позднее 30 дней.</p>
    <p>Если вы считаете, что ваши права нарушены, вы можете обратиться в
    Роскомнадзор как в уполномоченный орган по защите прав субъектов
    персональных данных.</p>
  `],

  ['cookie', 'Cookie и как их отключить', `
    <p>Сайт использует cookie — небольшие файлы, которые браузер сохраняет на
    вашем устройстве. Они нужны, чтобы счётчик статистики отличал нового
    посетителя от вернувшегося и чтобы сайт запоминал ваши настройки
    отображения.</p>
    <p>Cookie можно отключить в настройках браузера или удалить уже
    сохранённые. Сайт продолжит работать, но часть настроек перестанет
    запоминаться. Отказаться именно от сбора статистики Яндекс.Метрикой можно
    с помощью официального блокирующего дополнения Яндекса.</p>
  `],

  ['bezopasnost', 'Как данные защищаются', `
    <p>Оператор принимает разумные организационные и технические меры для
    защиты данных от неправомерного доступа, копирования, распространения и
    уничтожения. Сайт работает по защищённому протоколу HTTPS, доступ к
    заявкам есть только у оператора.</p>
  `],

  ['izmeneniya', 'Изменения политики', `
    <p>Оператор вправе изменять эту политику. Новая редакция вступает в силу с
    момента публикации на этой странице, если не предусмотрено иное. Дата
    последнего обновления указана в начале документа — сверяйтесь с ней.</p>
  `],
];

export function render() {
  const crumbs = breadcrumbs([['/politika/', 'Политика конфиденциальности']]);

  /* Оглавление: документ длинный, а искать в нём приходят с конкретным
     вопросом — обычно «как отозвать согласие». Без навигации это скролл
     вслепую. */
  const toc = SECTIONS
    .map(([id, title]) => `        <li><a href="#${id}">${title}</a></li>`)
    .join('\n');

  const sections = SECTIONS.map(([id, title, html]) => `      <h2 id="${id}">${title}</h2>
${html.trim()}`).join('\n\n');

  /* Реквизиты выводим только теми полями, которые реально заполнены в
     layout.mjs. Строка вида «ИНН: не указан» подрывает доверие сильнее, чем
     её отсутствие. */
  const requisites = [
    ['Оператор', OPERATOR.name],
    OPERATOR.status && ['Статус', OPERATOR.status],
    OPERATOR.inn && ['ИНН', OPERATOR.inn],
    ['Сайт', `<a href="${SITE}/">karatsapov.ru</a>`],
    ['Почта', `<a href="mailto:${OPERATOR.email}">${OPERATOR.email}</a>`],
    ['Телефон', `<a href="tel:${OPERATOR.phoneTel}">${OPERATOR.phone}</a>`],
  ].filter(Boolean)
    .map(([k, val]) => `          <div class="req-row"><span class="req-k">${k}</span><span class="req-v">${val}</span></div>`)
    .join('\n');

  const body = `<header class="hero" data-screen-label="Политика">
  <div class="wrap">
    ${crumbs.visible}
    <div class="eyebrow">Документ</div>
    <h1>Политика обработки <span class="accent">персональных данных</span></h1>
    <p class="lead">Что происходит с именем и телефоном, которые вы оставляете в форме на этом сайте: зачем они нужны, куда попадают, сколько хранятся и как их удалить.</p>
    <p class="policy-updated">Редакция от <time datetime="${UPDATED_ISO}">${UPDATED_HUMAN}</time></p>
  </div>
</header>

<section class="section">
  <div class="wrap wrap-narrow">
    <nav class="policy-toc reveal" aria-label="Содержание документа">
      <div class="policy-toc-title">В документе</div>
      <ol>
${toc}
      </ol>
    </nav>

    <div class="prose reveal" style="margin-top:40px;">
${sections}
    </div>

    <div class="policy-req reveal">
      <div class="policy-req-title">Реквизиты оператора</div>
      <div class="policy-req-grid">
${requisites}
      </div>
    </div>
  </div>
</section>

<section class="section cta-final">
  <div class="wrap" style="text-align:center;">
    <h2>Остались вопросы по данным?</h2>
    <p class="lead" style="margin:16px auto 28px; max-width:560px;">Напишите — отвечу лично. Удалю данные по первой просьбе, без объяснения причин.</p>
    <div class="btn-row" style="justify-content:center;">
      <a class="btn btn-fill btn-lg" href="mailto:${OPERATOR.email}">Написать на почту</a>
      <a class="btn btn-ghost btn-lg" href="/contacts/" data-lead-modal>Все контакты</a>
    </div>
  </div>
</section>`;

  const schema = [
    crumbs.schema,
    {
      '@context': 'https://schema.org',
      '@type': 'WebPage',
      name: meta.title,
      description: meta.description,
      url: SITE + meta.path,
      inLanguage: 'ru-RU',
      dateModified: UPDATED_ISO,
      publisher: { '@id': SITE + '/#person' },
      about: { '@type': 'Thing', name: 'Обработка персональных данных' },
    },
  ];

  return { body, schema };
}
