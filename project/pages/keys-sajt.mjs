/* /keysy/sajt-marketologa/ — кейс про этот же сайт.

   Отдельная страница, а не блок на /razrabotka-sajtov/. Причина не в
   объёме: сайт, на котором человек прямо сейчас находится, — единственный
   проект в портфолио, который он может проверить сам, не веря на слово.
   Такому доводу нужна своя страница, а не карточка в ряду других проектов.

   Про цифры. Все они считаются по репозиторию и сборке: число страниц,
   статей, строк кода, вес. Никаких «100 из 100 в PageSpeed» — замер
   скорости зависит от сети и устройства, а проверить его владелец не
   сможет. То, что можно проверить открыв сайт, — можно писать.
*/
import { SITE, breadcrumbs, faqItems, nOf } from '../layout.mjs';
import { CASE_CSS, GALLERY_CSS, facts, workBlocks, stackRow,
  gallery, heroFrame, lightboxScript } from '../case-kit.mjs';

/* Все числа кейса — в одном месте, и сборка их сверяет с репозиторием
   (checkSelfCaseNumbers в build.mjs). Кейс про сайт, который не даёт
   страницам разойтись с данными, не может сам держать цифры руками:
   добавится статья — текст соврёт, и заметит это читатель, а не автор. */
export const N = {
  pages: 63,          // страниц в карте сайта
  posts: 18,          // статей в блоге
  casePages: 24,      // страниц кейсов, все выведены на витрину /keysy/
  fromData: 21,       // из них собираются из cases-data.mjs
  niches: 4,          // отраслевых страниц
  codeLines: 21604,   // строк в .mjs/.jsx/.js/.css
  commits: 138,       // правок в истории репозитория
};

const PAGE = ['страница', 'страницы', 'страниц'];

export const meta = {
  path: '/keysy/sajt-marketologa/',
  title: `Кейс: сайт маркетолога — ${nOf(N.pages, PAGE)} и сборка своими руками`,
  description: `Как устроен karatsapov.ru: генератор статики на Node вместо CMS, ${nOf(N.pages, PAGE)} из данных, автоматические проверки вёрстки перед деплоем. Кейс, который можно проверить, просто открыв сайт.`,
  ogImage: '/assets/og/razrabotka-sajtov.jpg',
};

/* Скриншоты сняты с боевой сборки, а не нарисованы: это тот же сайт, что
   открыт у читателя, и расхождение заметили бы сразу. */
const HERO_SHOT = {
  src: '/assets/dev/site/glavnaya.webp', w: 1600, h: 1000,
  alt: 'Главная страница сайта интернет-маркетолога: заголовок, цифры опыта и портрет',
  cap: 'Главная: первый экран с цифрами по опыту',
};

const SHOTS = [
  { src: '/assets/dev/site/keysy.webp', w: 1600, h: 1000,
    alt: 'Витрина кейсов с фильтрами по услуге и нише',
    cap: `Витрина: ${nOf(N.casePages, ['кейс', 'кейса', 'кейсов'])}, фильтры по услуге и нише` },
  { src: '/assets/dev/site/kejs.webp', w: 1600, h: 1000,
    alt: 'Страница отдельного кейса: задача, что сделано, цифры результата',
    cap: `Страница кейса: таких ${N.fromData}, и все из одного файла данных` },
  { src: '/assets/dev/site/nisha.webp', w: 1600, h: 1000,
    alt: 'Отраслевая страница для ресторанов с графиком загрузки зала по дням недели',
    cap: 'Отраслевая страница: у каждой свой разбор механики ниши' },
  { src: '/assets/dev/site/blog.webp', w: 1600, h: 1000,
    alt: 'Лента блога с фильтрами по темам',
    cap: `Блог: ${nOf(N.posts, ['статья', 'статьи', 'статей'])}, фильтры по темам, оглавление в каждой` },
];

const FACTS = [
  [String(N.pages), 'страницы в сборке'],
  [String(N.posts), 'статей в блоге'],
  [String(N.casePages), 'страницы кейсов'],
  [(N.codeLines / 1000).toFixed(1).replace('.', ',') + ' тыс.', 'строк кода'],
];

const BLOCKS = [
  {
    tag: 'Архитектура',
    h: 'Генератор статики вместо CMS',
    lead: 'Сайт собирается скриптом на Node: данные лежат в файлах, страницы получаются из них. Ни базы, ни админки, ни PHP — отдаётся готовый HTML.',
    items: [
      `Каждая страница кейса собирается из одного файла с данными — сейчас их ${N.fromData}: правится текст в данных, перестраиваются страница, витрина, карта сайта и блоки «кейсы рядом».`,
      `Отраслевые страницы (${N.niches}) — из общего каркаса: одинаковая вёрстка, у каждой свой разбор механики ниши.`,
      'Статьи блога пишутся в Markdown, лента, RSS и оглавления строятся сами.',
      'Меню и подвал существуют в одном экземпляре и подставляются в страницы на сборке.',
      'Нечего ломать снаружи: нет админки, нет входа, нет базы данных.',
    ],
  },
  {
    tag: 'Проверки',
    h: 'Сборка падает, если страница получилась плохой',
    lead: 'Самая ценная часть проекта: ошибки ловятся до выкладки, а не через месяц, когда их находит клиент.',
    items: [
      `Все ${nOf(N.pages, PAGE)} открываются в браузере на десктопе и телефоне: проверяются зоны нажатия, горизонтальная прокрутка и ошибки в консоли.`,
      'Пиксельный поиск тёмных полос на стыках блоков — глазом такое замечается не всегда, а на телефоне бросается в глаза.',
      'Ловушка мёртвых анимаций: элемент, который навсегда остался прозрачным из-за неверного контейнера прокрутки.',
      'Сверка меню и подвала между двумя источниками: разошлись — сборка падает и показывает, где именно.',
      'Проверка ссылок на посадочные у каждой услуги: без адреса кнопка «Подробнее» просто не рисуется, и это видно только глазами.',
    ],
  },
  {
    tag: 'Скорость',
    h: 'Картинки и разметка приведены к виду, который не тормозит',
    lead: 'Скорость здесь не самоцель, а следствие: чем меньше отдаётся, тем быстрее открывается.',
    items: [
      'Снимки переведены в WebP — минус 2,6 МБ без заметной потери качества.',
      'Размеры каждой картинки проставляются на сборке автоматически, из заголовка самого файла: текст не прыгает при загрузке.',
      'Картинки, на которые не осталось ни одной ссылки, в сборку не едут — минус ещё 4,4 МБ.',
      'Шрифты подключены с преднагрузкой, чтобы шапка не «дышала» при подмене шрифта.',
      'Анимации фона отключаются на слабых устройствах и вне видимой области экрана.',
    ],
  },
  {
    tag: 'SEO',
    h: 'Разметка и структура — из данных, а не руками',
    lead: 'Всё, что поисковик читает машинно, генерируется вместе со страницей и потому не расходится с ней.',
    items: [
      'Хлебные крошки, описание услуги и блоки вопросов размечены микроразметкой на всех страницах, где они есть.',
      'Карта сайта собирается на каждом деплое, дата изменения берётся из данных страницы, а не из даты коммита.',
      'Заголовки страниц укорачиваются автоматически, если не влезают в выдачу: хвост с доменом отбрасывается, а не режется текст.',
      'Статьи связаны между собой по темам: подборка «читайте также» считается по общим тегам, а не берёт три последние.',
    ],
  },
  {
    tag: 'Деплой',
    h: 'Пуш в репозиторий — и сайт обновился',
    lead: 'Ручных шагов нет: правка приезжает на сервер сама, если прошли проверки.',
    items: [
      'GitHub Actions собирает сайт и синхронизирует его с сервером.',
      'На сервере nginx: сжатие, кэширование по типам файлов, редиректы со старых адресов.',
      'Сертификат продлевается сам, проверка домена лежит вне каталога сайта — чтобы синхронизация её не стёрла.',
      'История правок — в репозитории: видно, что и когда менялось, и можно вернуться назад.',
    ],
  },
];

const STACK = ['Node.js', 'React (JSX + Babel)', 'Terser', 'Markdown', 'Playwright',
  'sharp', 'GitHub Actions', 'nginx', 'Яндекс Метрика'];

const FAQ = [
  ['Почему не WordPress или конструктор?',
   `Потому что задача другая. Конструктор хорош, когда страниц десяток и их правит нетехнический человек. Здесь ${nOf(N.pages, PAGE)}, из которых ${N.fromData} собирается из одного файла с данными, — руками это не поддерживается. Плюс у генератора статики нет админки и базы, а значит нечего ломать и нечего обновлять ради безопасности.`],
  ['Вы всем делаете такие сайты?',
   'Нет. Этот сайт — витрина возможностей, а не шаблон. В большинстве проектов нужна посадочная под рекламный запрос, и она делается проще и быстрее. Генератор со сборкой из данных оправдан там, где страниц много и они однотипные: каталог, кейсы, филиалы, отраслевые страницы. <a href="/razrabotka-sajtov/">Как это выглядит в работе</a>.'],
  ['Сколько занял проект?',
   `Он не заканчивался: сайт живёт и меняется постоянно — ${N.commits} правок в истории репозитория на момент публикации кейса. Первая рабочая версия собралась за несколько дней, всё остальное — блог, кейсы, отраслевые страницы, проверки — дописывалось по мере надобности.`],
  ['Зачем проверки, если сайт делает один человек?',
   'Именно поэтому. Ревьюера нет, а ошибки одинаковые: тёмная полоса на стыке блоков, кнопка меньше пальца, ссылка в никуда после переименования страницы. Проверки ловят это до выкладки. Половина из них появилась после того, как я сам сломал ровно эту вещь и не заметил.'],
  ['Можно посмотреть, как всё устроено внутри?',
   'Часть решений описана в <a href="/blog/">блоге</a>, а результат виден прямо здесь: любая страница сайта — это то, о чём кейс. Открыть исходники целиком не могу, там лежат рабочие данные и настройки.'],
];

const OWN_CSS = `<style>
${CASE_CSS}
${GALLERY_CSS}

/* ── Что было и что стало ────────────────────────────────────────────────
   Не «до/после» в смысле дизайна, а смена подхода: ручная правка против
   сборки из данных. Две колонки с разными маркерами — самый прямой способ
   это показать, не рисуя схем. */
.was { display: grid; gap: 1px; margin-top: 28px; background: var(--line);
  border: 1px solid var(--line); border-radius: 18px; overflow: clip; }
@media (min-width: 860px) { .was { grid-template-columns: 1fr 1fr; } }
.was-c { background: var(--surface); padding: 24px 26px; }
.was-h { display: flex; align-items: center; gap: 10px; margin-bottom: 16px;
  font-size: 12px; font-weight: 700; letter-spacing: .09em; text-transform: uppercase; }
.was-c.old .was-h { color: var(--txt-3); }
.was-c.new .was-h { color: var(--accent-bright); }
.was-l { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 11px; }
.was-l li { position: relative; padding-left: 24px; font-size: 15px; line-height: 1.55; color: var(--txt-2); }
.was-l li::before { position: absolute; left: 0; font-weight: 700; }
.was-c.old .was-l li::before { content: '—'; color: #ff8a80; }
.was-c.new .was-l li::before { content: '✓'; color: var(--accent-bright); }
</style>`;

export function render() {
  const crumbs = breadcrumbs([['/keysy/', 'Кейсы'], [meta.path, 'Сайт маркетолога']]);

  const body = `<header class="hero" data-screen-label="Кейс: этот сайт">
  <div class="wrap">
    ${crumbs.visible}
    <div class="case-hero">
      <div>
        <div class="eyebrow">Кейс · сайт, который вы сейчас читаете</div>
        <h1>Сайт маркетолога: ${nOf(N.pages, PAGE)}, <span class="accent">которые собираются сами</span></h1>
        <p class="lead">Единственный проект в портфолио, который можно проверить, не веря на слово: вы уже на нём. Здесь нет CMS и админки — страницы собираются из файлов с данными скриптом на Node, а сборка не выпустит наружу страницу, которая не прошла проверки.</p>
        <div class="btn-row">
          <a class="btn btn-fill btn-lg" href="/contacts/" data-lead-modal>Обсудить свой сайт</a>
          <a class="btn btn-ghost btn-lg" href="/razrabotka-sajtov/">Разработка сайтов</a>
        </div>
      </div>
      ${heroFrame(HERO_SHOT, 'karatsapov.ru')}
    </div>
    <div class="dev-stats" style="margin-top:38px;">
${facts(FACTS)}
    </div>
  </div>
</header>

<section class="section">
  <div class="wrap">
    <div class="eyebrow reveal">Задача</div>
    <h2 class="reveal">Сайт, который растёт, а вести его некому</h2>
    <p class="lead reveal">Личный сайт специалиста начинается как одностраничник и почти всегда им остаётся: добавить кейс, статью или страницу под новую услугу — это ручная правка вёрстки, и рано или поздно она перестаёт делаться.</p>
    <p class="lead reveal">Дальше происходит одно и то же. Цены на странице услуги и в прайсе расходятся. Меню на новой странице отличается от старой. Кейс есть в списке, но ссылка ведёт в никуда. Каждая мелочь по отдельности не смертельна, а вместе они читаются как «человеку всё равно» — на сайте, который продаёт внимание к деталям.</p>
    <div class="was reveal">
      <div class="was-c old">
        <div class="was-h">Как обычно</div>
        <ul class="was-l">
          <li>Новая страница — копия старой с правками руками</li>
          <li>Цена меняется в трёх местах, вспоминается одно</li>
          <li>Меню правится в каждом файле отдельно</li>
          <li>Ошибку в вёрстке находит посетитель</li>
          <li>Через год сайт страшно трогать</li>
        </ul>
      </div>
      <div class="was-c new">
        <div class="was-h">Как здесь</div>
        <ul class="was-l">
          <li>Страница кейса появляется из записи в файле данных</li>
          <li>Цена лежит в одном месте, сборка сверяет остальные</li>
          <li>Меню и подвал существуют в единственном экземпляре</li>
          <li>Ошибку находит проверка до выкладки</li>
          <li>Правка занимает минуты и не ломает соседнее</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<section class="section">
  <div class="wrap">
    <div class="eyebrow reveal">Как выглядит</div>
    <h2 class="reveal">Скриншоты разделов</h2>
    <p class="lead reveal">Витрина кейсов, страница отдельного кейса, отраслевая страница и лента блога — четыре типа страниц, каждый собирается из своих данных.</p>
    <div class="shots reveal">
${gallery(SHOTS)}
    </div>
  </div>
</section>

<section class="section works-section">
  <div class="wrap">
    <div class="eyebrow reveal">Что сделано</div>
    <div class="wk-flow">
${workBlocks(BLOCKS)}
    </div>
  </div>
</section>

<section class="section">
  <div class="wrap">
    <div class="eyebrow reveal">Стек</div>
    <h2 class="reveal">На чём собрано</h2>
    <p class="lead reveal">Ничего экзотического: инструменты выбраны по принципу «переживёт смену версий и не потребует поддержки ради самой поддержки».</p>
    <div class="stack-row reveal">
${stackRow(STACK)}
    </div>
    <p class="case-note reveal">Скриншоты выше сняты автоматически той же связкой, которой сайт проверяется перед выкладкой, — поэтому они не устаревают незаметно.</p>
  </div>
</section>

<section class="section">
  <div class="wrap wrap-narrow">
    <div class="eyebrow reveal">Вопросы</div>
    <h2 class="reveal">Что обычно спрашивают</h2>
    <div class="faq-list reveal">
${faqItems(FAQ)}
    </div>
  </div>
</section>

<section class="section">
  <div class="wrap">
    <div class="cta-final reveal">
      <h2>Нужен сайт, который не развалится через год?</h2>
      <p class="lead">Расскажите, что должно быть на сайте и кто будет его вести. Если хватит простой посадочной — так и скажу: генератор со сборкой из данных нужен далеко не всем.</p>
      <div class="btn-row">
        <a class="btn btn-fill btn-lg" href="/contacts/" data-lead-modal>Обсудить проект</a>
        <a class="btn btn-ghost btn-lg" href="/keysy/">Другие кейсы</a>
      </div>
    </div>
  </div>
</section>

${lightboxScript([HERO_SHOT, ...SHOTS])}`;

  const schema = [
    crumbs.schema,
    {
      '@context': 'https://schema.org',
      '@type': 'Article',
      headline: `Сайт маркетолога: ${nOf(N.pages, PAGE)}, которые собираются сами`,
      description: meta.description,
      author: { '@type': 'Person', name: 'Даниил Карацапов', url: `${SITE}/about/` },
      publisher: { '@type': 'Person', name: 'Даниил Карацапов' },
      mainEntityOfPage: SITE + meta.path,
      about: 'Разработка сайтов',
      articleSection: 'Кейсы',
    },
    {
      '@context': 'https://schema.org',
      '@type': 'FAQPage',
      mainEntity: FAQ.map(([q, a]) => ({
        '@type': 'Question',
        name: q,
        acceptedAnswer: { '@type': 'Answer', text: a.replace(/<[^>]+>/g, '') },
      })),
    },
  ];

  return { body, schema, extraHead: OWN_CSS };
}
