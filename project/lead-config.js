/* Доставка заявок с форм в Telegram.

   ЗНАЧЕНИЯ ПУСТЫЕ НАМЕРЕННО. Этот файл отдаётся браузеру как есть — он
   лежит по адресу https://karatsapov.ru/lead-config.js и открывается любым
   человеком без всяких инструментов. Токен бота, стоявший здесь раньше,
   был публичным с первого дня: по нему можно было прочитать все входящие
   заявки через getUpdates, писать от имени бота и завалить чат мусором.

   Клиентский код в принципе не может хранить секрет: всё, что доходит до
   браузера, доходит и до постороннего. Обфускация, склейка из кусков,
   base64 — не помогают, это те же данные в другом виде.

   ВАЖНО: удаление токена отсюда его не отзывает. Старый токен остаётся
   рабочим, пока владелец не выпустит новый через @BotFather (/mybots →
   бот → API Token → Revoke current token). До отзыва он лежит и в истории
   этого репозитория, и в кэшах поисковиков.

   Как вернуть доставку в Telegram по-человечески: бот дёргается не из
   браузера, а с сервера. Форма отправляет заявку на свой эндпоинт
   (serverless-функция или скрипт на том же VPS), эндпоинт держит токен в
   переменной окружения и уже он ходит в Bot API. Тогда токен не покидает
   сервер, а заодно появляется место для защиты от спама.

   Пока значения пустые, форма открывает WhatsApp с уже заполненным текстом
   заявки — доставка не ломается, ни одна заявка не теряется. */
window.LEAD_TG_TOKEN = '';
window.LEAD_TG_CHAT  = '';
