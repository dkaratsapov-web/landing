{
  "name": "landing",
  "version": "1.0.0",
  "description": "Лендинг Даниила Карацапова — статический сайт, собирается из JSX.",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && cp -r project dist && node project/build.mjs project dist",
    "check": "node project/check.mjs",
    "check:seams": "node project/check-seams.mjs"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-react": "^7.24.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "terser": "^5.30.0"
  },
  "dependencies": {
    "marked": "^15.0.12",
    "sharp": "^0.34.5"
  }
}
